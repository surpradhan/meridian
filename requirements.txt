# Base runtime dependencies for MERIDIAN
# Extended by requirements-dev.txt and requirements-prod.txt

# Core Framework
# fastapi 0.115.6 clears PYSEC-2024-38 and pulls starlette 0.41.x, which clears
# the multipart DoS CVE-2024-47874. NOTE: the deeper starlette advisories
# (CVE-2025-54121, CVE-2025-62727, PYSEC-2026-161) need starlette >=0.47/1.0 and
# a coordinated fastapi bump — those are deferred (see issue #13), not cleared.
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1

# Langchain Ecosystem
langchain==0.2.0
langchain-openai==0.1.7
langchain-community==0.4.2
langchain-groq>=0.1.0
langgraph==0.1.1
langsmith==0.1.63

# LLM & Tools
openai==1.25.0
tenacity==8.2.3

# Data & Database
sqlalchemy==2.0.50
pandas==2.1.2
psycopg2-binary==2.9.12

# HTTP client (used by app and tests)
httpx==0.28.1

# Configuration & Environment
python-dotenv==1.2.2  # CVE-2026-28684

# Logging
python-json-logger==2.0.7

# Authentication & Security
python-jose[cryptography]==3.5.0  # PYSEC-2024-232/233, PYSEC-2025-185
bcrypt==4.2.1  # pinned (auth-critical; was unbounded >=4.0.0)

# Export (Phase 7)
openpyxl>=3.1.0

# Visualization (Phase 8)
plotly>=5.18.0
